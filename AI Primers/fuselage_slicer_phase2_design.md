# CuraEngine — Fuselage Slicer
## Phase 2 Design Document

**Status:** Draft &nbsp;|&nbsp; **Date:** June 2026 &nbsp;|&nbsp; **Author:** Rszalay  
**Repository:** `Rszalay/CuraEngine` — branch `fuselage-slicer`  
**Predecessor:** Wing Slicer, branch `wing-slicer-5.11`

---

## Table of Contents

1. [Overview](#1-overview)
2. [Structural Concept](#2-structural-concept)
3. [Geometry & Parameterisation](#3-geometry--parameterisation)
4. [Arc Assignment Logic](#4-arc-assignment-logic)
5. [FDM Pitch Adhesion Constraint](#5-fdm-pitch-adhesion-constraint)
6. [Implementation Plan](#6-implementation-plan)
7. [Exposed Parameters](#7-exposed-parameters)
8. [Model Constraints & Validation](#8-model-constraints--validation)
9. [Recommended Cura Settings](#9-recommended-cura-settings)
10. [Future Work](#10-future-work)
11. [References](#11-references)

---

## 1. Overview

This document describes the design and implementation plan for Phase 2 of the CuraEngine aircraft parts fork: a fuselage slicer that generates pseudo-geodesic internal structure for 3D printed RC aircraft and UAV fuselages.

Phase 1 delivered a rib-plane infill for single-wall aircraft wings, with procedural diagonal rib lines sweeping across the wing cross-section and spar pocket wall removal. Phase 2 extends this approach to fuselage construction using a helical stringer pattern inspired by Barnes Wallis's geodetic airframe principle.

### 1.1 Motivation

Conventional 3D printed fuselages use either solid infill (heavy), gyroid infill (lightweight but not load-path-optimised), or pure hollow shells (weak). None exploit the structural efficiency available from a semi-monocoque geodetic approach.

The Wellington bomber fuselage — built from two counter-winding helical families of load-bearing members — demonstrated that torsional loads in opposing spiral members cancel each other out, producing structures that remain functional even after significant localised damage. This document describes how to replicate that principle in FDM 3D printing by generating the helix families procedurally in the slicer rather than encoding them in the STL geometry.

### 1.2 Relationship to Phase 1

| | Phase 1 | Phase 2 |
|---|---|---|
| **Structure generated** | Rib planes sweep along span axis | Helical stringers sweep along longitudinal axis |
| **Wall treatment** | Spar pocket wall removal | Inner cavity wall is STL-modelled; slicer does not modify it |
| **Input geometry** | Consistent wing profile | Arbitrary closed polygon per layer |
| **Shared** | CuraEngine 5.11 fork; same parameter exposure pattern; same build system ||

---

## 2. Structural Concept

### 2.1 Pseudo-Geodesic Semi-Monocoque

A true geodesic airframe requires members that follow great-circle paths on a curved surface and intersect such that torsional loads cancel. This implementation is **pseudo-geodesic** because:

- Members are straight chords in each print layer, not true geodesic curves on the fuselage surface
- The helix approximates geodesic behaviour by rotating the chord pattern incrementally with each layer
- No explicit transverse members are generated — the counter-rotating helix families serve as mutual transverse restraint at their crossing nodes

The result is structurally analogous to a geodetic frame but manufacturable on a standard FDM printer with no support material and no post-processing.

### 2.2 Load Path Logic

The outer single-wall skin carries hoop stress in tension. The radial walls of the corrugated stringer carry crushing and shear loads across the annulus. At the troughs, the stringer welds to the inboard face of the outer skin, bracing it against inward buckling — this is the primary mechanism by which the internal structure supports the skin. At the peaks, the stringer runs along the inner cavity wall, distributing load circumferentially at the inner boundary.

Where CW and CCW stringer families cross in the annulus, a structural node is formed that transfers load between families — this is the mechanism by which torsion resistance is achieved.

The inner cavity wall is **explicitly modelled in the STL** and is printed as a normal shell wall by CuraEngine without any modification. The slicer does not generate, suppress, or alter it. The stringer generated by Phase 2 occupies the annular space between the outer skin and this inner wall.

> **⚠ Critical constraint:** Hoop stiffness is low with a single-wall outer skin. Bulkhead layers at key Z positions (battery bay, wing tube socket, landing gear attachment) restore circumferential stiffness at load concentration points. See Section 6.5.

### 2.3 Comparison to Conventional Infill

| Strategy | Weight | Torsion | Notes |
|---|---|---|---|
| Solid infill | Heavy | Good | Not suitable for fuselages |
| Gyroid / lightning | Light | Isotropic | No preferred load path |
| Hollow shell only | Lightest | Poor | Buckles under point loads |
| **Phase 2 helical stringer** | **Light** | **Excellent** | **Structurally intentional** |

---

## 3. Geometry & Parameterisation

### 3.1 Coordinate System

All geometry is computed in **print space**, centred on the bounding box of the fuselage mesh at each layer. The longitudinal axis is Z (print direction). The fuselage is assumed to print upright (nose or tail up).

Any print orientation is technically supported — the slicer is relative to the bounding box centre, not the model's local axes — but non-upright orientations produce non-intuitive stringer paths. The bounding box centre is recomputed per layer to follow any curvature or taper along Z.

### 3.2 Input Geometry — Two Polygon Modes

The fuselage STL is a **shell model** with both surfaces explicitly modelled in CAD. Depending on the Z station, each layer presents one of two cases:

#### Two-polygon layers (annular section)
The slicer sees **two closed polygons** — an outer skin perimeter and an inner cavity perimeter. The inner cavity is the hollow core of the fuselage, modelled directly into the STL. **The slicer does not generate the inner cavity wall.** It is treated as a pre-existing boundary that the stringer path uses as its inner rail.

The stringer lives entirely within the **annular region** between the two polygons.

#### Single-polygon layers (solid section)
At Z stations where the inner cavity has not yet begun or has ended (nose cone, tail cone, solid bulkhead zones), only the outer perimeter polygon is present. There is no inner boundary. In these zones the **bounding box centroid** acts as a degenerate inner boundary — a point rather than a curve. See Section 3.6.

### 3.3 Corrugation Cross-Section — The Square Wave Model

The stringer is not a chord across the section. It is a **square wave cross-section swept helically** around the annulus. Think of an old-fashioned corrugated cardboard edge profile: alternating flat segments at two different radii, connected by short radial walls.

The four segment types that compose one full corrugation cycle are:

```
  outer skin ──── radial wall ──── inner perimeter ──── radial wall ──── outer skin
  (trough)         (rising)           (peak)            (falling)         (trough)
```

Wrapped into a circle and rotated incrementally per layer, this produces the helical corrugated stringer.

#### Segment type 1 — Outer skin contact (trough)
The stringer runs **parallel to and just inboard of the outer skin polygon**, offset by one line width (`skin_offset`, default = `nozzle_diameter`). This offset is intentional: the extruded line must be close enough to the outer wall to **weld to it** on the inboard face, bracing the skin against inward buckling, but must not overlap the outer wall path itself. The trough segment subtends the arc between two adjacent radial walls.

#### Segment type 2 — Radial wall (rising and falling)
The stringer turns **radially inward** (or outward), traversing the full depth of the annulus from the outer skin offset to the inner perimeter. On two-polygon layers this is the distance between the two polygon boundaries. On single-polygon layers this is the distance from the outer skin offset to the centroid. The radial wall is the primary structural element — it resists crushing loads and transfers shear between inner and outer boundaries.

#### Segment type 3 — Inner perimeter arc (peak)
The stringer runs **along the inner perimeter polygon** for one arc segment, connecting the top of one radial wall to the top of the next. This is a true arc-follow on the inner polygon, not an approximation. The segment length is determined by the spoke spacing at the inner perimeter.

On **single-polygon layers** (Section 3.6), there is no inner perimeter polygon. The path reaches the centroid and immediately pivots to the next outbound radial wall — a V-shape rather than a U-shape.

### 3.4 Arc-Length Perimeter Sampling

**N evenly-spaced sample points** are placed along the **outer wall polygon** using **arc-length parameterisation** — not angular sampling from the centroid. This ensures uniform corrugation pitch regardless of cross-section shape.

```
Algorithm:
  1. Compute total perimeter length P of the outer wall polygon
  2. Walk polygon edges, accumulating arc length
  3. Interpolate a point at each multiple of (P / N)
  4. Result: N radial wall attachment points on the outer skin for this layer
```

A corresponding set of N points is then projected onto the **inner perimeter polygon** (or centroid, for single-polygon layers) by casting rays inward from each outer sample point through the bounding box centre. These become the inner attachment points — the peaks of the corrugation.

> **Note:** Angular sampling from the centroid clusters points at corners and thins them on flat faces. Arc-length sampling on the outer polygon is required for non-circular cross-sections.

### 3.5 Phase Anchoring

Independent per-layer sampling produces an arbitrary starting point for each layer's point array, causing stringer paths to scramble rather than helix. Phase anchoring fixes this:

- A **reference ray** is cast from the bounding box centre at a fixed angle (default: 0° = +X axis, configurable via `phase_anchor_angle`)
- The outer perimeter walk always begins at the polygon point closest to that ray intersection
- The incremental rotation per layer is applied as an **index offset** into the point array — not a geometric rotation of the polygon itself

This produces a stable, continuous helix regardless of how the polygon changes shape between layers.

### 3.6 Single-Polygon Layers — Centroid Mode

At Z stations where no inner cavity polygon exists, the stringer cannot run along an inner perimeter arc because there is none. Instead:

- The inner attachment point for every radial wall is the **bounding box centroid** of that layer
- After traversing inward to the centroid, the path **immediately pivots** and traverses outward along the next radial wall — no dwell, no arc segment
- The result is a **V-shape** (two radial walls meeting at a point) rather than the U-shape (two radial walls bridged by an inner arc) of the two-polygon case

This transition happens automatically as the slicer detects the presence or absence of a second polygon at each layer. No user input is required.

> **Note:** The transition zone — layers where the inner cavity is just beginning or just ending — may produce short inner arc segments as the cavity polygon grows from a near-point. This is geometrically correct and requires no special handling.

### 3.7 Helix Pitch

Helix pitch is expressed as **degrees of rotation per mm of Z height**, independent of layer height. This produces consistent visual results across different print resolutions.

- `360 / fuselage_length_mm` → one full helix cycle over the entire fuselage
- Higher pitch → more corrugation cycles per unit length, more radial walls, more skin contact points
- Lower pitch → fewer cycles, longer skin and inner arc segments, lighter structure — but subject to the FDM adhesion constraint in Section 5

---

## 4. Arc Assignment Logic

### 4.1 Model

The perimeter of each layer's cross-section is divided into **N arcs** — the segments between adjacent sampled spoke endpoints. For each arc, the two spiral families independently make a claim:

- **S1** — CW helix family: `true` if this arc contains a CW spoke endpoint, `false` otherwise  
- **S2** — CCW helix family: `true` if this arc contains a CCW spoke endpoint, `false` otherwise  
- **U** — the resolved arc state: `true` = skin-side (the arc is printed as part of the outer wall contact zone), `false` = cavity-side (the arc is open to the hollow interior)

The two families are offset by N/2 spokes, so their "on" arcs interleave around the perimeter. They are never both `true` on the same arc simultaneously in the nominal case, but the operator still determines behaviour in edge cases and controls the overall coverage density.

### 4.2 Operator Analysis

Four boolean operators were evaluated. Only OR and XOR are implemented.

#### U = OR(S1, S2) — dense coverage

`U = S1 | S2`

Fires wherever **either** family claims an arc. Since the two families interleave, OR produces near-continuous skin contact with only small cavity windows between spoke endpoints. 

- **Skin coverage:** High
- **Character:** Dense, conservative, maximum adhesion surface
- **Weight:** Heaviest of the two implemented modes
- **Use case:** Large-diameter fuselages where hoop stiffness is a concern; verification and debugging (confirms spiral geometry is correct before opening structure up)

#### U = XOR(S1, S2) — alternating basket-weave

`U = S1 ^ S2`

Fires where **exactly one** family claims an arc. Produces a clean alternating pattern of skin-side and cavity-side arcs as you traverse the perimeter — each family owns its own set of perimeter nodes, and they interleave like a basket weave.

- **Skin coverage:** Medium — roughly half the perimeter is skin-side
- **Character:** Structurally honest geodetic pattern; maximum interior void; minimum weight
- **Weight:** Lightest of the two implemented modes
- **Use case:** Default. Best approximation of true geodetic alternating attachment

#### U = AND(S1, S2) — rejected

Fires only where both families simultaneously claim the same arc. Since the families are offset, this almost never occurs. Result is an extremely sparse, near-empty structure — **not implemented**.

#### U = NAND(S1, S2) — rejected

Complement of AND — fires almost everywhere. Degenerates toward a solid ring. **Not implemented.**

### 4.3 Crossing Points

Where CW and CCW chord lines intersect in the **interior** of the cross-section, a structural node is formed. The arc assignment operator governs the perimeter only — the crossing points are in the interior and are not directly affected by U.

In both OR and XOR modes, both families' chord lines are printed through the interior, and they physically touch and fuse at their crossing point mid-span. This passive node formation is likely sufficient for the target application.

> **Future work:** Crossing-node detection — computing the 2D intersection of CW and CCW chords within the polygon, and emitting an overextrusion pulse at those points to form an explicit structural node. This is tracked separately from arc mode and would be a third parameter (`node_reinforce`).

### 4.4 Operator Summary

| Operator | Formula | Skin coverage | Weight | Implemented |
|---|---|---|---|---|
| OR | `S1 \| S2` | High | Heavier | ✅ |
| XOR | `S1 ^ S2` | Medium | Lighter | ✅ |
| AND | `S1 & S2` | Near zero | — | ❌ |
| NAND | `!(S1 & S2)` | Near total | — | ❌ |

---

## 5. FDM Pitch Adhesion Constraint

### 5.1 The Problem

FDM requires that adjacent extruded lines be close enough to fuse — typically within **1.5× the nozzle diameter** laterally. For helical stringers, each layer's chord lines are rotated slightly relative to the previous layer's. If the pitch is too slow (rotation per layer too small), consecutive layers' chord lines are nearly parallel and nearly collocated, but do not share endpoints — they print into air and fail to adhere.

Counterintuitively, **too slow a pitch breaks adhesion** — the lines are close in angle but miss each other laterally just enough to not fuse. This is the dominant failure mode for this structure on an FDM printer.

### 5.2 Minimum Pitch Formula

For an outer skin arc segment of length `L` (the trough segment between two adjacent radial walls) and a per-layer rotation of `θ` degrees, the lateral displacement of that segment between adjacent layers is:

```
d ≈ L × sin(θ_per_layer)  ≈  L × θ_per_layer × (π / 180)    [for small θ]
```

For adhesion: `d ≤ k × nozzle_diameter`, where `k ≈ 1.5` (empirical FDM fusion threshold).

Rearranging:

```
θ_min_per_layer  =  arcsin( k × nozzle_diameter / L )  ×  (180 / π)

pitch_min (°/mm)  =  θ_min_per_layer / layer_height
```

Where:
- `L` = outer arc segment length at this layer (outer perimeter / spoke_count, approximately)
- `nozzle_diameter` = from Cura machine settings
- `layer_height` = from Cura print settings
- `k` = 1.5 (hardcoded default, exposed as `adhesion_factor` for advanced users)

### 5.3 Arc Segment Length Estimation

The outer skin arc segment length between adjacent radial walls is approximately:

```
L_approx  ≈  outer_perimeter / spoke_count
```

For arbitrary polygons, the actual minimum arc segment length is computed per layer from the arc-length sampled point set (the shortest gap between any two adjacent sample points). The slicer uses this minimum as the conservative estimate for the adhesion check.

### 5.4 Taper Interaction

On a tapered fuselage, chord length decreases toward the narrow end. This **tightens** the minimum pitch constraint toward the tail — the same `helix_pitch` setting that satisfies adhesion at the widest fuselage section may violate it at the tail. 

The validation pass (Section 8.2) checks the pitch constraint at every layer and warns at the first layer where it is violated, so the user knows exactly which fuselage station is the limiting constraint.

### 5.5 Maximum Pitch

There is also a practical upper bound: too fast a pitch produces chord crossings with a wide angular separation, and the unsupported span between crossing nodes grows. When that span exceeds the printer's bridging capability (~40–60 mm for most setups), interior chord lines sag.

```
pitch_max (°/mm)  =  arcsin( bridge_span_max / L ) × (180/π) / layer_height
```

This is a softer constraint than the minimum — sagging chords are weak but the print does not fail catastrophically. It is flagged as a warning, not an error.

### 5.6 Validation Behaviour

| Condition | Severity | Action |
|---|---|---|
| `pitch < pitch_min` at any layer | **Error** | Warn with first violating layer Z and the minimum required pitch at that station |
| `pitch > pitch_max` at any layer | Warning | Warn with first violating layer Z and estimated bridge span |
| Pitch within bounds at all layers | OK | No output |

---

## 6. Implementation Plan

### 6.1 CuraEngine Integration Points

Phase 2 follows the same architectural pattern as Phase 1, modifying CuraEngine's infill generation path rather than the slicing or wall generation stages.

A new infill pattern type `FuselageGeodesic` is added alongside the existing rib pattern, selected via `fuselage_enable`. When active for a given layer, standard infill generation is skipped and the corrugated stringer path is generated and injected in its place. The outer skin wall and inner cavity wall (where present) are printed normally by CuraEngine — the slicer does not alter wall generation for either boundary.

The key inputs to the stringer generator at each layer are:
- **Outer polygon** — the outer skin perimeter, from CuraEngine's normal wall detection
- **Inner polygon** — the inner cavity perimeter if present, detected as a second closed polygon inside the outer at this layer; `null` if absent (single-polygon layer)

### 6.2 Modified Files

| File | Change |
|---|---|
| `src/infill.cpp` | Add `FuselageGeodesic` case; perimeter sampling; corrugation path generation; arc assignment operator |
| `src/FffGcodeWriter.cpp` | Inner/outer polygon detection per layer; bulkhead layer detection |
| `include/settings/types/` | New enum types if needed |
| `dist/fdmprinter.def.json` | Parameter exposure to Cura UI |
| `dist/install.ps1` | Updated installer |

### 6.3 Arc Assignment Implementation

S1 and S2 are computed as independent boolean arrays over the N arc segments before the operator is applied. This separation keeps the geometry debuggable — each array can be logged independently.

```cpp
// Compute S1 (CW family) and S2 (CCW family) arc ownership
std::vector<bool> s1(spoke_count, false);
std::vector<bool> s2(spoke_count, false);

for (int i = 0; i < spoke_count; ++i) {
    s1[(cw_phase_index + i) % spoke_count]  = true;
    s2[(ccw_phase_index + i) % spoke_count] = true;
}

// Apply operator
std::vector<bool> u(spoke_count);
for (int i = 0; i < spoke_count; ++i) {
    switch (arc_mode) {
        case ArcMode::OR:  u[i] = s1[i] | s2[i]; break;
        case ArcMode::XOR: u[i] = s1[i] ^ s2[i]; break;
    }
}
```

### 6.4 Pitch Validation Implementation

The pitch check runs once per layer before stringer generation. The relevant span length `L` is the **outer skin arc segment length** between two adjacent radial walls — this is the trough segment that must fuse to its counterpart on the adjacent layer. It uses the minimum arc segment length across the current layer:

```cpp
float L_min = *std::min_element(outer_arc_lengths.begin(), outer_arc_lengths.end());
float theta_min = std::asin(adhesion_factor * nozzle_diameter / L_min) * (180.0f / M_PI);
float pitch_min = theta_min / layer_height;

if (helix_pitch < pitch_min) {
    log_warning("Fuselage slicer: pitch %.2f °/mm below adhesion minimum %.2f °/mm at Z=%.2f mm "
                "(min arc segment %.2f mm). Increase helix_pitch or reduce spoke_count.",
                helix_pitch, pitch_min, layer_z, L_min);
}
```

### 6.5 Bulkhead Layers

At Z heights in `bulkhead_positions`, the fuselage slicer substitutes a near-solid concentric ring:

- Detect when current layer Z is within one layer height of a bulkhead position
- Generate concentric infill at `bulkhead_density` (default 95%) for that layer only
- Resume helical generation on the next layer with phase continuity preserved

> **Note:** Bulkhead positions are not auto-detected. They must be set manually at wing tube sockets, battery bay ends, motor bulkhead, and landing gear attach points.

### 6.6 Dual Helix Family

When `dual_helix` is enabled (default: `true`), two passes of chord generation run per layer — CW and CCW. The CCW family is offset by N/2 spokes from the CW family, producing crossing nodes at roughly the geometric midpoints of the CW chords.

When disabled, only the CW family is generated. Lighter and faster to print, but no torsional node network — suitable only for very light fuselages or tail booms.

---

## 7. Exposed Parameters

All parameters follow the Phase 1 exposure pattern via `fdmprinter.def.json`.

| Parameter | Default | Type | Description |
|---|---|---|---|
| `fuselage_enable` | `true` | bool | Master toggle |
| `spoke_count` | `16` | int | Number of radial walls per full corrugation cycle. Controls corrugation density. |
| `helix_pitch` | `2.0` | float °/mm | Rotation of corrugation pattern per mm of Z height |
| `dual_helix` | `true` | bool | Generate both CW and CCW stringer families |
| `arc_mode` | `xor` | enum | `xor` — alternating (default); `or` — dense coverage. See Section 4. |
| `phase_anchor_angle` | `0.0` | float ° | Reference ray angle for perimeter walk phase anchoring |
| `skin_offset` | `nozzle_diameter` | float mm | Inboard offset of trough segment from outer skin. Must be close enough to weld. |
| `bulkhead_positions` | `[]` | list mm | Z heights where solid ring substitutes corrugated stringer |
| `bulkhead_density` | `95` | int % | Infill density at bulkhead layers |
| `adhesion_factor` | `1.5` | float | Fusion threshold multiplier for pitch adhesion check (k). Advanced. |

---

## 8. Model Constraints & Validation

### 8.1 Required Model Properties

| Constraint | Effect if violated |
|---|---|
| Fuselage prints upright | Non-upright prints produce non-intuitive stringer paths (not prevented) |
| Simple closed polygon at every layer | Holes or self-intersections cause degenerate chord generation |
| Cross-section evolves gradually along Z | Abrupt steps cause phase discontinuities |
| Centroid lateral drift < 10% of section diameter per layer | Larger drift causes stringer paths to wander unpredictably |

### 8.2 Validation Pass

Runs once before infill generation. Emits warnings to CuraEngine's standard log — does not halt slicing.

| Check | Threshold | Severity |
|---|---|---|
| Centroid lateral drift | > 10% of local section diameter between adjacent layers | Warning |
| Cross-section area change | > 40% between adjacent layers | Warning |
| Arc segment length vs nozzle | Min arc segment < 3× nozzle diameter (corrugation too fine to print) | Warning |
| Pitch vs adhesion minimum | `helix_pitch < pitch_min` at any layer | **Error-level warning** |
| Pitch vs bridge maximum | `helix_pitch > pitch_max` at any layer | Warning |
| Single-polygon transition | Inner cavity polygon appears or disappears between adjacent layers | Info — triggers centroid mode |

---

## 9. Recommended Cura Settings

| Setting | Value |
|---|---|
| Wall Line Count | 1 |
| Top Layers | 0 (4 at nose/tail caps) |
| Bottom Layers | 4 |
| Infill Density | 0% — fuselage slicer overrides infill |
| Surface Mode | Normal |
| Print Speed | −20% from standard for outer wall |
| Cooling | Maximum |
| Material | LW-PLA (lightest); PETG (toughest); PLA (acceptable) |

---

## 10. Future Work

- **True geodesic arcs** on tapered sections — variable pitch as a function of local radius, producing genuine great-circle paths rather than chord approximations
- **Nodal reinforcement** — overextrusion pulse at CW/CCW interior crossing points to form explicit structural nodes (`node_reinforce` parameter)
- **Automatic bulkhead detection** from mesh features — recesses, through-holes, and boss features at attach points detected and converted to bulkhead layers automatically
- **Validation warnings in Cura UI** — surface log warnings as Cura UI notifications via plugin
- **Phase 3** — tail surface slicer combining Phase 1 rib logic with Phase 2 fuselage attachment geometry

---

## 11. References

- Barnes Wallis, US Patent 2060387 — *Aircraft Body Structure* (1936)
- Wikipedia: [Geodetic airframe](https://en.wikipedia.org/wiki/Geodetic_airframe) — structural principle and Wellington bomber history
- `Rszalay/CuraEngine`, branch `wing-slicer-5.11` — Phase 1 implementation reference
- Xiao & Cirak, *Infill topology and shape optimisation of lattice-skin structures*, [arXiv:2105.04017](https://arxiv.org/abs/2105.04017)
- CuraEngine internals documentation: [github.com/Ultimaker/CuraEngine/wiki/Internals](https://github.com/Ultimaker/CuraEngine/wiki/Internals)

---

*Rszalay / CuraEngine &nbsp;|&nbsp; Confidential &nbsp;|&nbsp; Draft — June 2026*
